#!/bin/sh
# コミットメッセージに層2（語彙）を当てる。コメントから追い出した履歴・事情は、放っておくと
# ここへ移る——公開リポジトリの恒久記録であり、あとから直せない。禁止語彙の源泉は esorp.yaml
# ひとつなので、このフックは正規表現を持たず、流し込むだけ。
#
# 有効にする: make hooks（core.hooksPath をこのディレクトリへ向ける）
set -e

go run ./cmd/esorp check --text - < "$1"
